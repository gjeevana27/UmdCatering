# Copy this to .streamlit/secrets.toml for local testing of the secrets
# path (optional — locally you can just use env vars instead, see
# .env.example). On Streamlit Cloud, paste these same values into your
# app's Settings -> Secrets box, in this same format.

GEMINI_API_KEY = "your-gemini-key-here"
APP_PASSCODE = "choose-a-shared-passcode"

# Paste the FULL CONTENTS of your Firestore service-account .json file
# between the triple quotes below, exactly as downloaded.
FIRESTORE_SERVICE_ACCOUNT_JSON = """
{
  "type": "service_account",
  "project_id": "...",
  "...": "...rest of the downloaded file..."
}
"""
